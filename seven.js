// log values with variable names smartly
const name = 'Mash';
const age = 30;
const isCaptain = true;

const captains = name === 'Mash' && isCaptain;
const ages = age > 30 ? 'old' : 'young';
const isCaptains = isCaptain ? 'yes' : 'no';
console.log(captain, age, isCaptain);
